<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#F8F6E9"
    tools:context=".Receiver_listViewReceiver">

    <!-- Ken XML file this gives us the instace of scrolling-->


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/GiverRecyclerView"
        android:layout_width="400dp"
        android:layout_height="500dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="100dp"
        android:layout_marginRight="10dp"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:padding="0dp"
        android:scrollbars="vertical" />

    <ImageButton
        android:id="@+id/imageView4"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="100dp"
        android:layout_marginTop="25dp"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/profile_edit_button" />

    <ImageView
        android:id="@+id/returnButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="25dp"
        android:layout_marginTop="25dp"
        app:srcCompat="@drawable/menu_button"
        android:onClick="onReturnButtonClick" />







</RelativeLayout>
